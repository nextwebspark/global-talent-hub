Replit Implementation Prompt — Fix Clockwork Project Selector

Update the Clockwork project selector so it reflects real Clockwork projects, not placeholders or partial data.

1. Remove Mock / Hardcoded Project Data

Remove any:

Static project arrays

Demo / sample project definitions

Cached fallback lists

The project selector must be populated only from the Clockwork API

2. Fetch Projects from Clockwork API (Authoritative Source)

Update the Clockwork integration to:

Fetch projects using the Clockwork Public API

Explicitly include:

Closed projects

Retained projects

Special projects

Do not filter by status unless explicitly requested by the user

If the API supports query parameters for status or type:

Include all statuses

Include all project types

3. Handle Pagination Correctly (Critical)

If the Clockwork API is paginated:

Fetch all pages

Aggregate results before displaying

Do not stop at page 1

Do not rely on default page size

Failure to do this will hide most real projects.

4. Permission Handling

Display only projects the authenticated API user has access to

If a project is locked or restricted:

Display it

But prevent enrichment if access is insufficient

Show a tooltip or warning instead of hiding it

5. Project Selector UI Requirements

Each project option must show:

Project title (exact Clockwork name)

Client company (if available)

Project status (Open / Closed)

Project type (Retained / Special)

Do not rename or abstract project names.

6. Validation & Debugging

Log:

Raw project count returned by Clockwork

Pagination page count

If zero or very few projects are returned:

Surface a warning in logs

Do not silently fall back to demo data

7. Enrichment Safety

Enrichment must still:

Be scoped to the selected project

Be read-only

Never write back to Clockwork

Acceptance Criteria (Must Pass)

The project list in the app matches the project list visible in Clockwork

Closed projects appear (as seen in Clockwork UI)

No “Demo” or fabricated projects appear

Selecting a project correctly scopes enrichment